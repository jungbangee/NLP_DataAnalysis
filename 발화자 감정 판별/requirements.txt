# Core ML Frameworks
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Transformers (KoBERT & Tokenizer)
transformers>=4.44.0
tokenizers>=0.19.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.5.0
joblib>=1.3.0

# Training utilities
tqdm>=4.66.0

# Optional: For configuration & experiment tracking
PyYAML>=6.0.1

# (Optional) visualization or debugging
matplotlib>=3.8.0

# (Optional) huggingface datasets (if later you cache tokenized text)
datasets>=2.20.0
